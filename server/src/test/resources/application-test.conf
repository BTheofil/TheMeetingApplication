ktor {
    application {
        modules = [ hu.tb.meet.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:sqlite:build/test.db"
    url = ${?TEST_DATABASE_URL}
}

jwt {
    realm = "meeting-server-test"
    issuer = "hu.tb.meet.test"
    audience = "meeting-users-test"
    expiresInMinutes = 60
}
